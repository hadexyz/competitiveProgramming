{
  "C++ Comp Prog": {
    "prefix": "cpcpp",
    "scope": "cpp",
    "body": [
      "#include <bits/stdc++.h>",
      "using namespace std;",
      "using ll = long long;",
      "using vll = vector<long long>;",
      "//2d matrix:vector<vector<long long>> vec(rows, vector<long long>(cols, 0)); // all zeros",
      "void solve()",
      "{",
      "\t$0",
      "}",
      "int main()",
      "{",
      "\tcin.tie(NULL);",
      "\tios_base::sync_with_stdio(false);",
      "\tint n=1;",
      "\t//cin >> n;",
      "\twhile(n--)",
      "\tsolve();",
      "\treturn 0;",
      "}"
    ],
    "description": "Comp prog C++ template"
  }
}
